javascript 
<script> 
속성 값 async 불리언 속성 존재하면 true,없으면 false 
-> defer 문서상의 순서를 따라 실행 async랑 비슷한효과 
nonce - script-scr ,content-security-policy에 스크립트를 등록하기위한 일회용 숫자 nonce 
일회용숫자값을 전송할때마다 생성해야됌
referrerpolicy : no-referrer Referer 헤더 전송x
no-referrer-when-downgrade(기본값) : 
if (조건식) { } // 참
else { } /거짓
.textContext , .innerText
textContext는 <script>와 <script> 요소를 포함한 모든 요소의 콘텐츠를 가져옵니다
반면 innerText는 사람이 읽을수있는 요소만 가져옴 
자바스크립트 변수 선언시 let , var  
javascript는 숫자값을 문자열로 변환 안됌
ex "37" - 7 // 30
"37" + 7 // 377
대신 문자열을 숫자값으로 가능 
parseInt() 정수만
parseFloat()